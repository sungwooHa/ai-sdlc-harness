---
name: __PREFIX__-ponytail
description: Apply Ponytail's reuse-first simplification to coding, refactoring and dependency choices without weakening this harness's agreement, artifacts or verification. Use when the user asks for ponytail, 포니테일, less code or less over-engineering; also when the implement adapter needs a simpler design.
---

Read the agreed spec/plan and affected code, then invoke the `ponytail` skill. Use the user's
requested lite/full/ultra level; otherwise use its full default. Apply these project overrides:

- Simplify implementation within the agreed outcomes. Reuse code, standard-library or native
  features before adding abstractions or dependencies. Do not deliver a smaller feature than
  requested; material scope changes return to agreement before implementation.
- The four applicable HTML views, agreement baseline, ADRs when needed, PR value review,
  accessibility, security and the project's required checks are requirements, not boilerplate.
  Do not delete or compress them to satisfy Ponytail's code-first/three-line output preference.
- Follow the repo's existing test tools and verification ladder. Ponytail's minimal runnable check
  supplements missing coverage; it never replaces required tests with an ad hoc self-check.
- Permission remains with the existing agreement gate. This skill grants no write access and
  cannot bypass protected paths, approval, scope or source-bound verification.
- When explaining a simplification, name its actual tradeoff. Keep ordinary updates short but
  provide the requested review and decision evidence in full. A Ponytail mode persists only as
  an agent preference; no upstream always-on or mode-tracking hooks are installed here.

If the user says stop ponytail or normal mode, stop applying this optional simplification layer.
The harness agreement and verification requirements still apply. Do not edit the vendored body;
record project-specific changes in this adapter and update the pinned source intentionally.
